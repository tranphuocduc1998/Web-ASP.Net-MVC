@model IEnumerable<ComicWeb.Areas.Admin.Models.Category_Comic>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ComicID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CategoryID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descriptions)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ComicID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descriptions)
        </td>
        <td>
            @*@Html.ActionLink("Sửa", "Edit", new {  id=item.ComicID, id2=item.CategoryID }) |*@
            @Html.ActionLink("Chi tiết", "Details", new { id = item.ComicID, id2 = item.CategoryID }) |
            @Html.ActionLink("Xóa", "Delete", new { id = item.ComicID, id2 = item.CategoryID })
        </td>
    </tr>
}

</table>
